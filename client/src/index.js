// This is entry point of react app its going contain main render 

// importing react from react this will les us use JSX
import React from 'react';
//this will allows us to rener 
import ReactDom from 'react-dom';
import App from './App';

ReactDom.render(<App/>,document.getElementById('root'));